# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "branches_put_success.csv"
    fields:
      - "cocNumber"
      - "city"
      - "cocBranchNumber"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: Validate successful API responses
    flow:
      - log: "Performing a POST request"
        post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            city: "{{ city }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
              hasProperty: [ "id", "email", "phone", "cocNumber", "cocBranchNumber", "tradeName", "eoriNumber", "deregistrationDate", "createdAt", "updatedAt" ]
            - statusCode: 201
              hasProperty: [ "id", "email", "phone", "cocNumber", "cocBranchNumber", "tradeName", "eoriNumber", "deregistrationDate", "createdAt", "updatedAt" ]
        think: 3
