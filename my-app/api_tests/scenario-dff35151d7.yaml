# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roost-gpt4-32k



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "scenario-dff35151d7.csv"
    fields:
      - accounts_body
    skipHeader: true

scenarios:

  # Scenario: Create a new account and verify
  - name: "Create new account and verify"
    flow:
      # POST request to /accounts for creating new account
      - post:
          url: "/accounts"
          json:
            email: "{{ accounts_body }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d" # Provide commit-hash value here
          expect:
            # Verify response status code and headers
            - statusCode: 201 
            - hasHeader: "api-version"
            - headerEquals: ["api-version","1.0.0"]
            - hasHeader: "commit-hash"
            - headerEquals: ["commit-hash","928d28d"]
          log: "Created new account"

      # Post request process-time 
      - think: 5

      # GET request to /me for verifying account creation
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Verify response status code, headers and body
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: ["api-version","1.0.0"]
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          log: "Verified the created account by fetching account details"
