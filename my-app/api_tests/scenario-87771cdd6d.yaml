# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roost-gpt4-32k



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "scenario-87771cdd6d.csv"
    fields:
      - "authorizationID"
    skipHeader: true
  processor: "./processor.js"
  environments:
    production:
      target: "http://localhost:8080"
      plugins:
        expect: {}
scenarios:
  - name: "Delete authorization"
    flow:
      # Scenario: Delete authorization
      # Given the authorization ID
      # When the client sends a DELETE request to "/authorizations/ID"
      # Then the response status code should be 200 for a successful delete
      - delete:
          url: "/authorizations/{{ authorizationID }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { "api-version" : "1.0.0" }
      # Adding delay using think
      - think: 5
      # And the authorizations should not appear in GET request for "/authorizations"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "authorizations"
            - notHasProperty: "{{ authorizationID }}"
            - hasHeader: "api-version"
            - headerEquals: { "api-version" : "1.0.0" }
      # Log the outcome
      - log: "The deleted authorization does not exist in the list of authorizations anymore."
