# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roost-gpt4-32k



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:  
    expect: {}
  payload:
    path: "scenario-803fe763f5.csv"
    skipHeader: true
    fields: 
      - email
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Grant authorization"
    flow:
    
      # Scenario: Create an account
      - post: 
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          capture:
            json: "$.id"
            as: "accountId"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasHeader: "api-version"
            - headerEquals: { "api-version": "1.0.0" }
            - matchesRegexp: { "commit-hash": "928d28d" }
          log: "Created an account, now authorize another account to access it"
          think: 1
          
      # Scenario: Grant Authorization
      - post:
          url: "/authorizations"
          json:
            email: "{{ email }}"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
          log: "Granted authorization to access account"
          think: 1
          
      # Scenario: Confirm access authorization via GET request.
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "accounts"
          log: "Confirmed that the account is authorized"
          think: 1
