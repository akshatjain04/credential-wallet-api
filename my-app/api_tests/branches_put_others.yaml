# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roost-gpt4-32k

# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_put_others.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      # Testing Post Request with all invalid payloads
      - post:
          url: "/recipients/branches"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "errorMsg"
          think: 2

      # Testing Post Request: Incorrect URL
      - post:
          url: "/wrongurl/branches"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 2

      # Testing Post Request: Missing Authentication
      - post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
          think: 2
